# Unhodl Signals

Public, append-only track record of crypto regime signals from https://unhodl.io.

One JSON file per UTC day, committed at the end of each daily cron (00:05 UTC).
Each file contains an array of signal events for that date.

This repository is the immutable audit trail for the Unhodl signal service.
